using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class SaveDataListItem : MonoBehaviour
{
    [SerializeField] private Image mapImage;
    [SerializeField] private TextMeshProUGUI townName;
    [SerializeField] private TextMeshProUGUI saveType;
    [SerializeField] private TextMeshProUGUI savedTime;
    [SerializeField] private Button selectButton;
    [SerializeField] private Button deleteButon;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
